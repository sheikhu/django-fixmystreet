$(function(){
    //used to be in reports_map.html with a lot of commented javascript that was cleaned up for lisibility
    // (just in case you need to check it)
    fms.map.addIncidentTypeControl();
    fms.loadAllIncidents();
});